<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
    <title>Document</title>
</head>
<body>
    <div class="w-full h-screen bg-zinc-900 text-white p-10">
        <div class="w-full flex justify-end">
            <a class="bg-red-500 rounded-md px-3 py-2 mb-5 inline-block text-sm" href="/logout">Logout</a>
        </div>
        <div class="flex items-start gap-3 mb-3">
            <div class="w-20 h-20 rounded-md">
                <img class="w-full h-full object-cover" src="/images/uploads/<%= user.profilepic %>" alt="">
            </div>
            <h2 class="text-3xl mt-12">Hello, <%= user.name %></h2>
        </div>
        
        <h5 class="mb-5">you can create a new post</h5>
        <form method="post" action="/post">
            <textarea placeholder="what's on your mind?" class="w-1/3 resize-none bg-transparent border-2 border-zinc-800 rounded-md block" name="content"></textarea>
            <input class="px-3 py-2 bg-blue-500 block w-40 mt-2 rounded-md" type="submit" value="Create new post">
        </form>

        <div class="posts mt-20">
            <h3 class="text-zinc-400">Your Posts.</h3>
            <div class="postcontainer mt-5 rounded-md">
                <% user.posts.reverse().forEach(function(post){ %>
                    <div class="post w-1/3 mb-5 p-4 border-2 border-zinc-700 bg-zinc-700 rounded-md">
                    <h4 class="text-blue-500 mb-2">@<%= user.username %></h4>
                    <p class="text-sm tracking-tighter"><%= post.content %></p>

                    <small class="mt-2 inline-block"><%= post.likes.length %> likes</small>
                    <div class="btns flex gap-4 mt-5">
                        <a class="text-blue-500" href="/like/<%= post._id %>">
                            <%= post.likes.indexOf(user._id) === -1 ? 'Like' : 'unlike' %>
                        </a>
                        <a class="text-zinc-300" href="/edit/<%= post._id %>">edit</a>
                    </div>
                </div>

                <% }) %>

                

            </div>
        </div>
        
    </div>
</body>
</html>